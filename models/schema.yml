version: 2
models:
  - name: dim_customer
    columns:
      - name: customer_id
        tests: [not_null, unique]
  - name: dim_product
    columns:
      - name: product_id
        tests: [not_null, unique]
  - name: fct_sales
    columns:
      - name: order_id
        tests: [not_null, unique]
